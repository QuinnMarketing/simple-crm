[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma db push && npx prisma db seed && node_modules/.bin/next start"
healthcheckPath = "/login"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
